# Design & plan docs moved

The `specs/` and `plans/` design documents that used to live here were moved to a
**private** repository because this repo (`ameriglide/it-admin`) is public and the
design docs contain internal tailnet topology (anchor IPs, the headscale control
URL, host inventory).

They now live in:

> **`ameriglide/it-admin-docs`** (private)

Request access from IT if you need them. New design/plan docs should be added
there, not here.
